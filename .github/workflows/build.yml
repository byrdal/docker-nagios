---
name: build

on:
  pull_request:
    branches: ['**']
  workflow_call:

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Build image
        run: docker build -t byrdal/nagios:latest .
